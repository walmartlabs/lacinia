{:interfaces
 {:Named
  {:fields {:name {:type String}}}}

 :objects
 {:Person
  {:implements [:Named]
   :fields {:name {:type String}
            :age {:type Int}}}

  :Business
  {:implements [:Named]
   :fields {:name {:type String}
            :employee_count {:type Int}}}}}
